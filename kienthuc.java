public class kienthuc {
    // so sanh chuoi
    chucVu.substring(0,2).compareTo("GD") == 0;
    // String >> int
    int i = Integer.parseInt("200");
    // so sánh xâu
    System.out.println(s1.equals(s2));//true  
    (o1.getMaSV()).compareToIgnoreCase(o2.getMaSV());
}
